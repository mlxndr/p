/**
 * Template system for reveal.js presentations
 * Loads common HTML parts and applies presentation-specific content
 */

// Let's avoid using the template system and instead directly include the necessary scripts
// This comment is for our reference only and stays in this file
// The problem with dynamically loading scripts is that it's hard to ensure proper initialization order

// DO NOT modify this file - it's not being used anymore
// Instead, update the template examples to include all necessary scripts directly